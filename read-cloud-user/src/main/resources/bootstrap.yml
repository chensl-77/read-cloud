spring:
  application:
    name: read-cloud-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yml #指定yml格式的配置
        group: DEFAULT_GROUP #分组
#        namespace: csl #命名空间ID


